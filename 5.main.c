#include "main.h"
#include <stdio.h>

int main(void)
{
    int resultado = 0;

    resultado = div(9,3);
    printf("El resultado de la division es: %d\n",resultado);
    resultado = div(8,6);
    printf("El resultado de la division es: %d\n",resultado);
    resultado = div(15,5);
    printf("El resultado de la division es: %d\n",resultado);
    return 0;
}
